Setup:

Modify your Flash Player Global Security Setting to 'Always Allow'.

To run:

Open Preview.html in Internet Explorer.
Click Start.
Be patient - it takes some time to load the first item.

Modify subtest or item xmls to test new features.

The build script in ../tdc copies TestClient.lzx.swf to this directory when the main or all targets are executed.